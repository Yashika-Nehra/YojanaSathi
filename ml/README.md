# ML

`train.py` builds a TF-IDF classifier over `data/ml_training.csv` and saves `ml/model.joblib`.

This is a small hackathon demo corpus, not a production benchmark. The saved model can only rank items that already passed rule-based eligibility.
